subprojects {
    group = 'tigase-jaxmpp'
    version = jaxmppVersion
}

allprojects {
    apply plugin: 'maven-publish'

    repositories {
        mavenLocal()
        maven {
            url "http://build.xmpp-test.net/maven/"
        }
        jcenter()
    }
}
